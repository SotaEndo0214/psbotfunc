export SLACK_AUTH_TOKEN=
export SLACK_SIGNING_SECRETS=
export POKEMONSLEEP_FOODS_JSON_PATH=
export POKEMONSLEEP_COOKS_JSON_PATH=
export GOOGLE_CLOUD_PROJECT=

if [ -e ".envrc.local" ]; then source .envrc.local; fi